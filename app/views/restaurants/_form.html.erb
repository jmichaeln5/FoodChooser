<%= form_with(model: @restaurant, local: true) do |form| %>
  <% if @restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant.errors.count, "error") %> prohibited this @restaurant from being saved:</h2>

      <ul>
      <% @restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_id, value: @user.id %>
  </div>

  <div class="field">
    <%#= form.label :user %>
    <%#= form. :user_id, id: :user_id %>
  </div>

 <%#= form.text_field :user, id: :user_id, value: @user.id %>


  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :restaurant_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
