<%= form_with(model: @restaurant, local: true) do |form| %>
<%#= form_with(model: [ @user, @user.restaurants.build ]) do |form| %>

  <% if @restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant.errors.count, "error") %> prohibited this @restaurant from being saved:</h2>
      <ul>
      <% @restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card text-center py-5">
  <div class="field">
      <%= form.hidden_field :user_id, id: :user_id, value: @user.id %>
  </div>

  <!-- <div>
    <%= "User ID" %>
    <br>
    <%= current_user.id %>
    <br>
    <% form.label :user_id %>
    <% form.text_area :user_id, id: :current_user_id %>
  </div> -->

  <div class="field">
    <strong> <%= form.label :title %>: </strong>
    <%= form.text_field :title, id: :restaurant_title %>
  </div>

  <br>

  <div class="actions">
    <%= form.submit  class: 'btn btn-success' %>
  </div>
<% end %>
</div>
