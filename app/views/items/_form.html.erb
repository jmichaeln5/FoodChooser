<%= form_with(model: [ @menu, @menu.items.build ]) do |form| %>

<%#= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="card text-center py-5">
    <div class="field">
      <strong> <%= form.label :item_name %>: </strong>
      <%= form.text_field :item_name, id: :item_item_name, :required => true   %>
    </div>
    <hr>

    <div class="field">
    <strong>  <%= form.label :price %>: </strong>
      <%= form.text_field :price, id: :item_price %>
    </div>
    <hr>

    <div class="field">
    <strong>  <%= form.label :description %>: </strong>
      <%= form.text_area :description, id: :item_description %>
    </div>
    <hr>
    <br>

    <div class="actions">
      <%= form.submit  class: 'btn btn-success' %>
    </div>
  <% end %>
</div>
